# RAG Learning

A project for learning RAG (Retrieval-Augmented Generation) with LangChain, LangGraph, LangSmith and ChromaDB

## Set ENV

Install `Rust` and `Cargo` for `chromadb` first

```bash
cd rag-learning
python -m venv .venv
source .venv/bin/activate
pip install uv
uv sync
```
